spring:
  application:
    name: gateway
  profiles:
    active: dev
  main:
    web-application-type: reactive
    allow-bean-definition-overriding: true
  cloud:
    config:
      uri: http://localhost:8888