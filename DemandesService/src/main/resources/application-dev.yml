spring:
  datasource:
    url: jdbc:postgresql://localhost:5225/demandes-service
    username: demandes-service
    password: demandes-service
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    open-in-view: false